// import { PrismaClient } from "@prisma/client";

// const prisma = new PrismaClient();

// export async function createAttachment(
//   attachmentURL: string,
//   type: "jpeg" | "png" | "pdf" | "svg" | "mp4" | "gif",
//   holderData: {
//     type: "task" | "featureRequest" | "bugReport" | "post";
//     id: string;
//   }
// ) {

//   await prisma.attachment.create({
//     data: { attachmentURL: attachmentURL, type,  },
//   });
// }

// ill do this shit later lets test fr first lmao
// testing now
//ill do it no worries
// cool thx
// i need to resign in because this isnt on localhost:8000 for me so cookies are dif
//oklk ill do it
